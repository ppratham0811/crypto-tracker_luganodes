"use client";

import { Box } from "@chakra-ui/react";
import React from "react";

const HomeRow = ({ crypto }: { crypto: Object }) => {
  return (
    <>
      <Box className="w-full grid grid-cols-8">
        
      </Box>
    </>
  );
};

export default HomeRow;
